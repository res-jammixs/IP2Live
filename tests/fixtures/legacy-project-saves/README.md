These are the two old development saves previously shipped in the project's
`Saves` directory. They are retained only as regression fixtures. Do not copy
them into game saves or import them into a new player's storage.
